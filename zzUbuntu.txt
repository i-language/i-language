デスクトップインストール。
sudo apt update
sudo apt install -y ubuntu-desktop


日本語環境構築
sudo apt update
sudo apt install -y ibus-mozc language-pack-ja
sudo dpkg-reconfigure locales
ja_JP.UTF-8 を選択して有効化します。

ibus restart
settingsのsystemのregion＆LangegaeをJapaneseに変更
